{
  "name": "echostream-node",
  "version": "0.1.0",
  "description": "EchoStream Node.js binding — 基于 QUIC 的双向 RPC / Event / Stream 实时通信框架",
  "main": "index.js",
  "type": "commonjs",
  "license": "MIT",
  "keywords": ["rpc", "quic", "streaming", "realtime", "websocket"],
  "files": ["index.js", "echostream-node.node", "README.md"],
  "scripts": {
    "build": "cargo build -p echostream-node --release",
    "test": "node test/server.test.cjs"
  },
  "engines": {
    "node": ">=18"
  }
}
